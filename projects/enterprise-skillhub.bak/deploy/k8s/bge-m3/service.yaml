apiVersion: v1
kind: Service
metadata:
  name: skillhub-bge-m3
  namespace: skillhub
  labels:
    app: skillhub-bge-m3
spec:
  type: ClusterIP
  selector:
    app: skillhub-bge-m3
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
