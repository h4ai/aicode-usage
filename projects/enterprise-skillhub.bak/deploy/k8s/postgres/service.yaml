apiVersion: v1
kind: Service
metadata:
  name: skillhub-postgres
  namespace: skillhub
  labels:
    app: skillhub-postgres
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app: skillhub-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
      protocol: TCP
