apiVersion: v1
kind: Service
metadata:
  name: skillhub-redis
  namespace: skillhub
  labels:
    app: skillhub-redis
spec:
  type: ClusterIP
  selector:
    app: skillhub-redis
  ports:
    - name: redis
      port: 6379
      targetPort: redis
      protocol: TCP
