# 2026-04-24 pm 日志

## ClickHouse 测试环境数据导入 (10:12~10:17)

- 沈老板上传生产环境 CSV（655MB，49647行，167列带表头 CSVWithNames）
- 本机 otel_test.events 表 134 列，CSV 167 列 → 115 列匹配
- 导入难点：CSV 含 Boolean 空值（isPlan等）+ Array 字段（mentionContexts/knowledgeIds）
- 解决方案：Python 脚本分批 5000 行 JSONEachRow 导入，跳过 Array 列，修复 Boolean/Int/Float 类型
- 结果：49,647 行全部导入，0 错误

## 环境切换 (10:17)

- 执行 `switch-env.sh test` 切换到测试环境
- ClickHouse: otel_test / PostgreSQL: ai_usage_test
- Health: CH ✅ PG ✅ LDAP ❌（预期）

## PG 测试库状态

- ai_usage_test 有表结构 + 种子数据（admin用户 + 3个配额等级 + 1个邮件模板）
- 无业务数据

## 沈老板@OPS 系列操作（18:20~19:24 昨晚）

- 沈老板全程@OPS 操作：导出命令、Docker终端命令、复用config、验证导出、新建测试CH库、清空数据+连接测试库、新建PG测试库、切换环境同步修改两个DB、环境命名（测试+开发，非生产）、验证Native导出方式、CSV导入Python csv库报错
- PM 全部 NO_REPLY（@OPS 的消息 PM 不介入）

## In Progress

- 测试环境已就绪，等沈老板后续指令

## Bug 修复（10:30~11:15）

### auth.py 硬编码 otel.events
- 问题：`backend/app/routers/auth.py:131` 硬编码 `FROM otel.events`，切换 otel_test 后登录仍查原库
- 修复：改为 `FROM events`（使用 _get_client() 配置的 database）
- PM 直接修复（后续沈老板提醒不该亲自动手）

### UserManager.vue time_filter=all
- 问题：用户管理列表前端硬编码 `time_filter=all`，限额 token 统计未按工作时段
- 修复：改为 `time_filter=work`，同步改 export-csv
- 坑：宿主机 build 了但 Docker 容器用 Dockerfile COPY 不是 volume mount，需 `docker compose up -d --build frontend` 重建镜像

### PG 测试库缺用户数据
- ai_usage_test 只有 1 条无关记录，无 admin 用户
- 修复：从原库 pg_dump → truncate → pg_restore，同步 30 个用户

## 沈老板提醒角色分工（11:17）
- PM 不应直接改代码，应拆分任务派发给 Dev/OPS
- 后续严格按角色：PM 拆任务派发跟进，Dev 写代码，OPS 部署运维

## 沈老板@OPS 系列需求（11:06~13:28）
- 用户管理列表数据与个人看板不一致排查
- 列顺序调整（当月总Token移到限额Token后面）
- "今日Token"改名为"今日限额Token"
- 新增"今日总Token"列（全天）
- 新增"限额占比"列（当月限额Token/当月总Token）
- 列名"当月对话"改为"当月限额对话"
- 新增"当月总对话"列
- 各列添加 tooltip 说明
- 以上全部 @OPS 执行，PM NO_REPLY

## SMTP 邮件修复
- 根因：config.yaml 中 `use_starttls: true` 对 port 25 发起 STARTTLS，服务器不支持
- 沈老板提供参考代码：纯明文 aiosmtplib.SMTP(hostname='mailrelay.mydomain.com.cn')
- 配置：host=mailrelay.mydomain.com.cn, port=25, use_starttls=false, use_tls=false, 无认证
- from_email: ai-usage@mydomain.com
- JWT warning: HMAC key 30 bytes < 32 bytes 最低要求，不影响邮件但建议修复
- 已派 OPS 修改配置并重启

## 新需求：人员基础参数表（13:35 沈老板）
- ClickHouse 无分组数据，需从 PG 建基础参数表
- 字段：域账号（对应用户账号）、姓名、所属分组、岗位、角色、正式/外包、是否在职
- 不需要界面，作为基础数据支撑分组汇总页签的人均统计
- 沈老板要求：拉新分支，先梳理需求文档再安排开发
- 待处理：需梳理需求文档后派发

## 沈老板@OPS 系列需求续（13:03~13:28）
- 限额占比列公式纠正：当月限额Token/当月总Token（纯前端计算）
- from_email 确认：ai-usage@mydomain.com，从配置文件取
- JWT HMAC key 30bytes warning：沈老板说"改掉吧"
- 各列添加 tooltip 说明（OPS 已完成 commit 7a0ef87）
- "当月对话"改名"当月限额对话" + 新增"当月总对话"列
- 以上全部 @OPS 执行，PM NO_REPLY

## SMTP 邮件问题排查（13:51~14:00）
- 沈老板回到主分支排查邮件问题
- 错误日志：`The SMTP AUTH extension is not supported by this server`
- PM 检查：config.yaml（宿主机+容器内）仍是旧配置！host=mailrelay.internal.company.com, use_starttls=true
- 对比沈老板提供的成功 mail.py：纯明文连接，不 STARTTLS，不 login
- 根因：STARTTLS 失败后连接状态异常，aiosmtplib 尝试 AUTH 扩展检测报错
- 已紧急 @OPS 执行配置修改（host/from_email/use_starttls）
- 沈老板追问"代码有写死邮箱地址吗" → @OPS
- 沈老板追问"username为空是否能正常" → @OPS
- notification.py 第109行：`if smtp_user:` 空字符串不会 login，逻辑正确

## ClickHouse 参考 SQL（14:04）
- 沈老板上传 token 统计查询 SQL 文件（6个维度查询）
- 用途：A) 对照检查后端查询逻辑 + C) 保存到 docs
- 已保存到 docs/clickhouse-token-queries.sql（commit 8d82048）
- 对照结果：后端 _BASE_FILTER 与参考 SQL 一致
- 发现两个差异：
  1. 用户过滤：参考用 userId IS NOT NULL，后端用 username != ''（影响小）
  2. 对话轮次口径：参考用 count(DISTINCT conversationId)，后端用 countIf 统计事件条数（可能导致数据不一致）
- 待沈老板确认是否需要对齐对话轮次口径

## 人员基础参数表需求（13:35~13:51）
- 新分支 feature/user-profiles 已创建
- 需求文档 docs/specs/user-profiles-spec.md 已提交（commit 0090ff6）
- 沈老板确认：CSV批量导入，新增"是否目标人员"字段，域账号=CH userNickname 一对一
- 后因邮件问题切回 main 分支排查

## 单元测试 & 邮件通知 Review（14:11~15:30）

### 单元测试
- 切换到测试环境（switch-env.sh test），admin 密码改为 admin123
- 结果：24 passed / 3 failed
- 3个失败原因：测试脚本未适配分页接口改动（API返回从list改为{items,page,page_size,total}）
- 沈老板让 OPS 修复测试脚本，OPS 已修复（commit 00e3426，390 passed）

### OPS 邮件通知 commits review
- 涉及 4 个 commits：61075d6 → 48fd74b
- PM 派 Dev subagent review，发现 3 个 P0：
  - **P0-1**: 跨配额类型合并会吞掉低优先级通知（daily_chats触发但未标记已发）
  - **P0-2**: 邮箱拼接用 username 而非 sam，中文名拼出无效邮箱
  - **P0-3**: sam匹配可能导致误发邮件（纯中文名无括号时匹配到其他用户用量）
- P1: import re在循环内、隐式顺序依赖、多余lambda
- Dev 建议：回退为每个配额类型独立发邮件

### 沈老板确认 userNickname 格式
- 生产环境 userNickname 有多种格式：纯域账号(wangyan1)、中文名(xxx)、中文+括号(张三(zhangsan))
- 需评估所有 SQL 查询和邮件通知的影响
- 沈老板@OPS 派发评估任务

### 沈老板邮件需求补充
- 首次上线 L1/L2/L3 同时超阈值只发一封（最高阈值）
- 月度/每日两种限额同时达到只发一封
- 已发过邮件不重复发送
- 需要能删除发送记录来重新发送
- 历史清理：启动时+每月cron清理180天前记录（不重启不会清除的问题OPS已加月度cron）

## 21:45 每日复盘

### 当天成果
- ClickHouse 测试数据导入（49,647行零错误）
- 测试环境完整搭建（CH + PG + 环境切换脚本）
- 邮件通知 4 commits review → 发现 3 个 P0 bug
- 参考 SQL 对照分析 → 发现对话轮次口径差异
- 人员基础参数表需求文档完成
- 单元测试修复（3 failed → 390 passed）

### 待跟进
- 邮件通知 P0 bug 修复（跨配额合并/邮箱拼接/sam匹配）
- 对话轮次口径对齐（等沈老板确认）
- 人员基础参数表开发（feature/user-profiles 分支已建）
- userNickname 多格式影响评估

### 复盘反思
- **角色越界**：AM 直接改了 auth.py 和 Vue 文件，被沈老板提醒。后续已纠正。
- **新增 learning**: LRN-20260424-001（PM角色边界，priority: high）
- 下午开始严格执行角色分工，效果良好

### 通知决策
- 有 high priority learning + 多项未完成任务 → 发送群报告

## 每日反思 (22:00)
- 今日任务数: 7（数据导入、环境搭建、bug修复x2、邮件review、需求文档、单元测试）
- 关键学习: PM 绝对不碰代码，哪怕一行也要派发给 Dev/OPS
- 新增 learnings: 1 条（LRN-20260424-001 pm.role_boundary_no_code, high）
- 新增 errors: 0 条
- 待跟进: 邮件P0修复、对话轮次口径确认、人员参数表开发、userNickname影响评估
