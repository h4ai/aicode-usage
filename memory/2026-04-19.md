# 2026-04-19 pm 日志

## AI Code Usage 看板 — 4/18 下午~凌晨进展

### 完成
- PO UAT 报告已生成并发送（PDF+HTML，12项全PASS，1 BLOCKED AD域）
- GitHub 仓库 `h4ai/aicode-usage` 创建并推送（含完整 README 部署文档）
- ClickHouse 实际表字段是 camelCase（`userId`, `eventCode`, `conversationId`），DDL 文件的 snake_case 是设计稿未同步
- conversationId 字段全空，对话次数只能统计轮次（msg_count），无法按会话去重
- QA E2E R2 dispatch 状态 processing，QA Agent 在线（heartbeat 10m），等待完成

### OPS 执行中的需求迭代（沈老板直接 @OPS 指挥）
1. 指标卡片加 tooltip 问号说明（请求次数/对话轮次/活跃天数等）
2. 配额调整：L1=2500万 L2=5000万 L3=1亿
3. 对话轮次限额（每日）：前后端同步修改
4. 个人页面去掉请求次数，保留 Token + 对话轮次
5. 管理员用户管理：新增今日Token/今日对话/本月对话列，红黄绿灰状态灯，分页20/50/100，CSV导出
6. 全局趋势加对话轮次图表
7. **工作时段配置**：默认 08:30-18:00 周一至周五，管理员可配置，所有报表加切换（全天/工作时段/非工作时段）
8. 个人看板+管理后台3页面都加工作时段切换
9. Bug：切换工作/非工作时段数据不变化（沈老板反馈，OPS 修复中）

### spex 三层方案决策
- 沈老板确认：砍 User Stories 层，保留 GEARS+AC → 三层：requirements.md → GEARS+AC → prd.json
- 理由：金融合规需要 GEARS 的可追溯性，User Stories 的 As a...I want... 对 AI 是废话
- TDD 验证体系写入 SKILL.md：L0编译 L1单测 L2集成（同步）→ L3 E2E（半同步）→ L4可观测+L5人工（异步）
- OPS 已 commit 三层+TDD 到 h4ai/spex (20cae4f)

### PM 通信改进
- 沈老板要求：OPS 已在这个群里，PM 直接在群里 @OPS 即可，不需要发到 OPS 监听群
- 但 @ 变蓝仍需 feishu_im_user_message + post + at 标签

### 关键标识
- GitHub: h4ai/aicode-usage (public), h4ai/spex (fork)
- ClickHouse: localhost:8123, db=otel, table=events, camelCase fields
- 配额：L1=2500万token/月+100对话/日, L2=5000万+200, L3=1亿+500

## 4/19 下午续 — AI Code Usage 大量 UI 迭代

### 沈老板直接 @OPS 的需求（PM NO_REPLY，OPS 直接响应）
密集迭代约 30+ 条需求变更，涵盖：

**个人看板**
- 关键指标新增"本周"维度，页签顺序改为 今日→本周→本月
- 模型分布图+使用明细 接入 time_filter（工作/非工作时段）
- 使用明细：去除模型/IDE筛选，改为最近7天/30天/自定义；分页；CSV与界面数据一致
- "请求次数"→"对话轮次"，total page 改中文
- 自定义日期查询限最近3个月
- 工作时段按钮加大+醒目颜色+?说明
- 登录状态显示用户名+退出功能（右上角，与工作时段同行）
- 关闭时段过滤后：隐藏切换器，只显示"全天" tag（无额外提示）

**管理员后台**
- 页签顺序：用户管理(默认)→全局趋势→用量排行→分组汇总→工作时段→配额管理
- 用户管理：去除时段过滤；字段改名（本月限额Token/今日限额对话 + 本月总Token/今日总对话）
- 全局趋势/用量排行：工作时段数据=非工作时段 bug 需修复
- 用量排行：过滤按钮靠右
- 部门汇总/用量排行：新增"月对话轮次"列
- "部门"全局改"分组"（7处：页签名/标题/列头/搜索等）
- 配额级别隐藏"日请求上限"
- 关闭时段过滤后管理后台也联动
- 登录状态+退出功能

**数据/后端**
- 为所有用户构造今天的测试数据（Token+对话随机）
- 本周起始=周一凌晨0点
- 工作时段配置调整后，tooltip动态更新，数据自动过滤
- 总分验证：work+non_work=all 三用户完全匹配

### OPS Spec + QA 回归
- OPS 整理了 12 US 的 Spec 文档 → `docs/specs/SPEC-2026-04-19-regression.md`
- QA dispatch 已投递 `AI-CODE-USAGE-REGRESSION-001.dispatch.json`

### GitHub 提交要求（沈老板 15:28）
1. 清理测试文档/截图，不提交 GitHub
2. 代码密码替换为 my_password 等占位符
3. docs/ 和 specs/ 合并为 docs/
4. 完善部署文档（README），方便 AI Agent 快速部署
5. Python 版本：项目 requires-python>=3.11，GitHub Actions 应配 3.11+

### 沈老板问 GitHub Actions Pylint 配置
- 发了模板配置（python 3.8/3.9/3.10），但项目实际需要 >=3.11
- PM 待回答（被 pre-compaction 打断）

## 4/19 20:30~20:48 — GitHub Actions CI 配置

### 完成
- OPS 配置了完整 CI workflow（ci.yml）：后端 Lint(ruff)+Mypy、前端 ESLint+vue-tsc+build、安全扫描(Gitleaks+pip-audit+npm-audit)、CodeQL SAST、Docker build
- Dependabot 已开启，自动提了多个依赖升级 PR（vite、vue-tsc、echarts、uvicorn 等）
- CodeQL setup 已通过 ✅

### 问题
- 旧的 pylint.yml workflow 未删除，缺 libldap2-dev 导致持续失败
- 已通知 OPS 删掉旧 pylint.yml，只保留新 ci.yml

### 沈老板确认的 CI 配置
- 前端：ESLint + eslint-plugin-vue + typescript-eslint（eslint.config.js flat config）
- 后端：Ruff + Mypy + pylint（需 apt-get install libldap2-dev libsasl2-dev）
- 安全：Gitleaks（密钥泄露）、pip-audit + npm audit（依赖漏洞）、CodeQL（SAST）
- Dependabot alerts + security updates
- 全部对 public 仓库免费
- Node 版本改为 24 LTS
- Python 版本 >=3.11

### GitHub 仓库
- h4ai/aicode-usage (public)
- 默认分支：main
- OPS 工作分支：ralph/ai-code-usage-dashboard

## 每日反思 (22:00)

### 今日任务总结
1. **AI Code Usage 大量 UI 迭代** — 沈老板直接 @OPS 密集下发 30+ 需求变更，PM 正确保持 NO_REPLY 不插手
2. **GitHub Actions CI 配置** — OPS 完成完整 CI pipeline（Ruff/Mypy/ESLint/vue-tsc/Gitleaks/CodeQL/Dependabot）
3. **旧 pylint.yml 清理** — 发现旧 workflow 持续失败，通知 OPS 删除
4. **spex 三层方案** — 沈老板确认砍 User Stories 层，保留 requirements→GEARS+AC→prd.json
5. **记忆记录** — 全天持续更新 memory，保持上下文连续性

### 反思三问

**1. 做对了什么？**
- 在沈老板直接 @OPS 密集迭代时，PM 正确保持 NO_REPLY，没有插嘴干扰。这符合 `pm.only_do_what_asked` 原则
- 记忆文件保持了非常详细的记录，包括具体配额数值、字段命名变更、页签顺序等细节，便于后续追溯
- CI 配置问题（旧 pylint.yml）发现后快速通知 OPS，没有自己动手修

**2. 做错了什么？**
- 沈老板 15:28 问 GitHub Actions Pylint 配置时，PM 被 pre-compaction 打断，未及时回复。虽然后续 OPS 接手解决了，但响应延迟不理想
- QA E2E R2 regression dispatch 投递后缺少跟踪——dispatch 状态未确认

**3. 模式识别**
- **沈老板直接指挥 OPS 模式**：本周已多次出现沈老板跳过 PM 直接 @OPS 下发需求的情况。这是合理的（UI 微调不需要 PM 中转），PM 应继续保持 NO_REPLY + 记录
- **CI/DevOps 配置渐趋成熟**：从最初的裸仓库到现在完整 CI pipeline + Dependabot，基础设施在快速完善

- 今日任务数: 5
- 关键学习: 沈老板直接指挥 OPS 时 PM 保持 NO_REPLY 是正确策略
- 新增 learnings: 1 条
- 新增 errors: 0 条
