# 2026-04-23 pm 日志

## AI Code Usage Dashboard 进展（08:08~15:14）

### OPS 完成的工作（昨晚~今早）
- PR #43 合并（PAT API 功能 + 生产防御 5 项 + Dev 架构评审 6 项修复）
- 390 tests passed
- PAT Dev 复核：6 处差距修复（2×P0 token格式/错误响应, 2×P1 审计日志/注释, 2×P2 待确认）
- 安全头 middleware 全局覆盖（3 行代码）
- Skill 创建完成：`aicode-usage-query`（3.6KB .skill 文件）

### 沈老板新需求/Bug（08:08~15:13，全部 @OPS）
1. PAT 需求拆任务后用 Ralph 流程开发（非 OPS 直接做）
2. Skill 改为 API 接口执行 Python 命令（确定性执行）
3. Skill 提交到本仓库 Skill 目录
4. 用户管理界面无数据 + 用量排行转圈
5. 普通用户 PAT 调管理员接口返回什么
6. 提交 PR
7. 昨天今天调整哪些需要更新部署与 README
8. PR #44 多余？
9. PR #43 已合并确认
10. 重新打包部署
11. 通知设置：检查间隔和邮件域名重启后丢失 → **改为只在后台 config 文件配置，前端去除**
12. 个人界面无 API Token 入口 + 首次登录数据空白
13. 前端右上角版本号（灰色小字，PR 后自动 bump）
14. 自签名证书 API 报错（Python 示例代码执行报错）→ 支持 HTTP
15. 用量排行对话轮次汇总 vs 明细数据不一致
16. OPS 不回复（PM 催促 @OPS）

### PM 工作
- 派 QA subagent（7a6f50f1）执行 PAT E2E 测试（前端 7 截图 + API 6 功能测试）—— 运行中
- 催促 OPS 回复积压消息（15:13）
- 沈老板 @OPS 消息全部 NO_REPLY（PM 规则）

### OPS Ralph 完成项（09:00 前后）
- API Token 入口 + 首次加载空白修复：commit 69ebcf8
- 检查间隔/邮件域名前端字段去除
- 版本号 + 自动 bump GitHub Actions：commit 6b6ee95

### 待跟进
- [ ] QA PAT E2E 测试结果（subagent 7a6f50f1，已跑 >40min）
- [ ] OPS 回复积压的 5 条消息
- [ ] 对话轮次汇总 vs 明细不一致排查
- [ ] 自签名证书/HTTP 支持
- [ ] PR #44 是什么，是否需要关闭

## 21:45 每日复盘

### 当日统计
- 完成：Dev 修复 3 件（Leaderboard/SMTP/Admin tab）+ OPS Ralph 3 件（Token入口/检查间隔/版本号）
- 进行中：QA PAT E2E 测试（subagent 状态未知）、ClickHouse P0 修复
- 待跟进：OPS 积压回复 5 条、对话轮次不一致、HTTP 支持、PR #44

### 今日无新增 .learnings 条目

### 关键标识
- PR #43: 已合并（PAT + 生产防御）
- PR #44: 待确认
- QA subagent: agent:qa:subagent:7a6f50f1-f87f-4e52-9864-53bfd788bfe3
- OPS session: 67% context, 活跃但响应延迟
- 沈老板私聊 chat_id: oc_0eabf05153557d2692fd9754337b00d8

## 16:08 — Dev 修复完成 + ClickHouse P0 发现

### Dev 修复 3 件（已部署）
1. **Leaderboard 时间范围统一** — commit `13d1f6c`，`_computeDateRange()` rangeMode='month' 返回 {start: 当月1日, end: today}
2. **SMTP 硬编码移除** — commit `d95cc9c`，移除 mailrelay.internal.company.com 黑名单，加 warning 日志
3. **Admin tab 刷新保持** — commit `742460b`，AdminView.vue 用 URL query `?tab=xxx` 持久化

### P0 发现：ClickHouse 单例并发问题
- 沈老板发来后端日志，大量 500 错误
- 根因：`clickhouse_connect` client 全局单例，FastAPI 线程池并发触发 `concurrent queries within the same session`
- 影响接口：quota/usage, metrics/model-distribution, metrics/detail, metrics/summary, metrics/trend, admin/departments, admin/leaderboard
- 修复方案：每请求新建 client 实例或用连接池
- 已在 summary 中记录为 open TODO

### 其他日志发现
- JWT HMAC key 30 bytes < 推荐 32 bytes
- APScheduler 间隔仍 60min（config PUT 30min 没生效到调度器）
- farben10 LDAP 登录失败 4 次（账号锁定 data 775）

## 每日反思 (22:00)

### 今日任务总览
1. **QA PAT E2E 测试派发** — 派了 subagent，但结果未跟进到底
2. **催促 OPS 回复** — 积压 5 条消息，催了一次
3. **Dev 修复 3 件确认** — Leaderboard/SMTP/Admin tab 都已部署
4. **ClickHouse P0 发现记录** — 记录了根因和修复方案
5. **沈老板 16 条需求/Bug 中继** — 大部分转发 OPS，NO_REPLY 规则执行

### 反思 3 问

**1. 做对了什么？**
- 21:45 复盘及时做了当日统计，待跟进列表清晰
- ClickHouse P0 根因分析记录详细，方便后续修复
- 对沈老板 @OPS 消息严格执行 NO_REPLY，没有越权代答

**2. 做错了什么？**
- QA subagent 派出去后跑了 40+ min 没跟进结果，状态不明
- OPS 积压 5 条消息只催了一次，没有升级或设 deadline
- 今天产出了大量待跟进项但没有优先级排序

**3. 模式识别**
- **OPS 响应延迟是持续问题** — 不是今天第一次了，需要建立 escalation 机制
- **subagent 派出去后缺乏跟进** — 每次派发应设超时检查点

### 今日统计
- 今日任务数: 5（直接执行） + 16（中继/记录）
- 关键学习: subagent 派发后必须设超时检查点
- 新增 learnings: 1 条
- 新增 errors: 0 条
