# 2026-04-21 pm 日志

## 12:39 Pre-compaction flush

### 沈老板规则（04-20 22:55 确认，重要！）
- 沈老板在本群 @PM 的所有问题，**只在本群回复，不转发到 OPS 群**
- 除非沈老板明确要求让 OPS 执行，否则不自动派发任务
- PM 被批评：不该把沈老板的指令直接转发 OPS 群 + 不该擅自让 OPS 开发

### SQL 性能优化（sql-perf 分支）
- OPS 完成 ClickHouse SQL 优化：PREWHERE、assumeNotNull、endsWith、Client 单例、合并查询
- PM 审查结论：**业务逻辑未变，全部纯性能优化**，可合并
- 基准测试（12K 数据）：整体 30.5% 提升，合并查询收益最大（67.3%）
- 沈老板要求 OPS 跑 5 轮取平均、提交 PR、合并到 main
- 200 个测试用户 + 10000 条数据已构造（80/20 分布）

### 需求文档相关
- L1 净化 requirements-model-pricing.md：已派发 OPS（待完成）
- 更新 requirements.md v0.7→v0.8：PM 错误派发（沈老板本意是让 PM 整理变更清单，不是让 OPS 改文档）
- 架构文档 architecture-model-pricing.md：OPS 尚未生成

### 配额邮件通知增强 v2.0（新需求）
- 沈老板 @OPS 提出：配额到阈值发邮件、多阈值（50/80/100%）、可配置间隔、模板可编辑、去重、预留关闭模型接口
- OPS 输出需求文档 v0.2，沈老板确认：1）预留关闭接口 2）HTML模板 3）三档可配置 4）新建 email_notifications 表替代旧表
- PM 列出 7 个模糊点待沈老板确认（定时方式、SMTP、失败处理、并发、清空含义、权限、钩子签名）
- 新分支：feature/quota-email-v2（待创建）

### Open TODOs
- [ ] L1 净化 + 架构文档拆分（OPS 执行中）
- [ ] 配额邮件通知 v2.0 需求文档模糊点确认
- [ ] sql-perf 分支已合并到 main
- [ ] QA E2E R2 仍在 processing
- [ ] OPS 各项 UI/数据需求（管理员界面、全局趋势、部门汇总等）

## 15:19 Pre-compaction flush #2

### 配额邮件通知 v2.0 开发进展
- OPS 输出需求文档 v0.3（docs/requirements-quota-email-v2.md）
- 沈老板确认 7 个模糊点：内置定时(APScheduler)、SMTP已有、重试3次、逐个发、CH只读、管理员都可改模板、钩子预留本系统API
- OPS 使用 Ralph 流程派发 Dev（dispatch: TASK-QUOTA-EMAIL-V2.dispatch.json）
- Dev 完成 US-017~022，338 tests (新增47)，分支 feature/quota-email-v2，commit dc9ae85
- OPS 代码审查通过，发现1个不阻塞小瑕疵（enabled字符串判断）
- OPS 分析测试缺口：§2.10 通知设置保存后端未实现 → 沈老板决定：4个配置项保存后端，手动重启生效
- 沈老板要求补充单元测试（OPS 执行中）

### SQL 性能优化
- sql-perf 分支 PM 审查通过（业务逻辑未变），沈老板批准合并到 main
- OPS 跑 5 轮基准测试取平均（沈老板要求）
- 200 测试用户 + 10000 条数据已构造

### 非沈老板请求
- ou_84ec36a72096f80d82b4a63d963e45f0 请求：把群所有对话记录保存打包发给他（需确认身份和权限）

## 16:25 Pre-compaction flush #3

### 配额邮件通知 v2.0 — 下午密集进展
- Dev 完成 US-017~022，OPS 代码审查通过，合并到 main
- OPS 补充单元测试（沈老板要求）
- OPS commit + 部署完成（commit 10a8dad），测试 353 pass
- README 同步更新（新增 notification 配置块、2张新PG表、目录树）

### Bug 修复（15:51~16:15 密集）
- **Admin 登录失败**：PM 定位 bcrypt hash 不匹配 → OPS 修复
- **测试用户登录失败**：LDAP socket refused 时返回 401 而非 503，前端 fallback 不触发 → OPS 修复（commit d90c788），LDAPSocketOpenError 单独捕获为 LdapUnavailableError→503
- **沈老板要求**：test-login fallback 加配置开关，生产环境关闭
- **通知设置阈值 100 输入框太小**：只显示 10% → OPS 修复
- **配额级别人数不对**：未找到级别默认 L1（前后台都要改）→ OPS 处理中
- **管理后台刷新报错**：加载模板失败 → OPS 处理中
- **通知设置占位符无提示**：OPS 已增加占位符说明表+点击复制
- **模板预览/保存失败** → OPS 处理中

### QA 任务派发（16:22）
- 沈老板要求 QA 对 feature/quota-email-v2 全部需求做 Playwright 截图验证
- 要求输出 PDF 报告发到群里
- PM 拆分 6 步详细任务，直接派发 QA dispatch（QA-QUOTA-EMAIL-V2.dispatch.json）
- 18 条测试用例 + 11 张截图 + API 验证 + PDF 报告

### 非沈老板请求
- ou_84ec36a72096f80d82b4a63d963e45f0 再次请求导出群聊消息 → PM 拒绝（隐私安全）

### 沈老板 @OPS 其他指令（PM 均 NO_REPLY）
- 更新部署说明文档
- PG 新增表是否自动创建
- 管理后台用户管理报错 + 分组汇总无数据（CH只读）

## 16:42 Pre-compaction flush #4

### 全量测试规划（16:30~16:40）
- 沈老板要求梳理所有需求，QA 做全量 E2E 测试
- 方案 B+C 确认：1 个 QA Agent 串行，新旧功能一起
- 需求梳理：requirements.md v0.7（10 模块 M1~M10）+ requirements-quota-email-v2.md v0.3（US-017~022）
- QA dispatch `QA-FULL-E2E`：40 条用例 + 19 张 Playwright 截图 + 18 个 API + PDF 报告
- 之前的 `QA-QUOTA-EMAIL-V2` dispatch 已 canceled
- DEV dispatch `DEV-API-FULL-TEST`：27 个后台 API 全量 pytest 验证 + docker logs 无错误
- **Git commit 规则**：只提交测试脚本，不提交截图和测试报告（沈老板明确要求）

### QA inbox 问题
- TASK-128-RETEST 从 #5 到 #102 共近 100 个 dispatch（每 10 分钟一个），阻塞新任务
- 沈老板让 OPS 清理

### 全部 27 个后台 API 端点清单
- auth: login, test-login
- health: /health
- metrics: summary, trend, model-distribution, detail, export.csv, working-hours-config
- quota: usage
- admin: quota-levels(GET/PUT), users(GET), users/{id}/level(PUT), trend, departments, leaderboard, users/export-csv, leaderboard/export-csv, working-hours(GET/PUT), email-template(GET/PUT), email-template/preview(POST), email-template/variables(GET), notification-config(GET/PUT)

## 16:44 Pre-compaction flush #5

### DEV-API-FULL-TEST 完成（16:37）
- 27/27 端点全部 PASS
- ⚠️ 已知问题：clickhouse_connect 单例 client 并发 ProgrammingError（concurrent queries within same session）
- 根因：`_get_client()` 返回全局单例，非线程安全
- 推荐修复方案 A：去掉单例，每次创建新 client（HTTP 开销极低）
- 方案 B：`threading.local()` 每线程一个 client
- 沈老板问"怎么修"，等确认是否让 OPS 修

### 沈老板 @OPS 消息（PM NO_REPLY）
- 问占位符为什么是空的、具体哪些可用

## 17:00~17:26 QA E2E + 代码审查 + SA调查

### 沈老板 @OPS 按优先级修复代码问题（17:00）
- PM 之前做了代码审查，产出 P0/P1/P2/P3 改进建议
- 沈老板指示 OPS 按优先级逐级修复，每级完成单测+接口测试后再 commit
- PM 回 NO_REPLY（@OPS 消息）

### QA E2E 全量测试（16:32~17:24）
- **R1（外网IP）**：QA heartbeat 没触发，spawn subagent 执行但用了 74.226.48.15 → ConnectTimeout，40用例全 BLOCKED
- **R2（localhost）**：改用 127.0.0.1 重跑，2m54s 完成
  - 19 张 Playwright 截图 ✅
  - 40 条测试用例 ✅
  - Git commit: `5d88e25`（测试脚本）+ `ea09476`（清理误提交的 .venv）
- **发现问题**：
  1. Admin 部分 API 返回 401「密码已变更，请重新登录」— `/admin/working-hours`, `/email-template*`, `/leaderboard`, `/trend`
  2. `PUT /api/admin/quota-levels/L1` 返回 422（schema 不匹配）
  3. LDAP health=error（内部错误，已知）

### QA heartbeat 不触发 → SA 调查派发
- dispatch: `SA-QA-HEARTBEAT-INVESTIGATION.dispatch.json`
- 调查方向：heartbeat interval、maxConcurrent、TASK-128-RETEST 大量 dispatch 阻塞、HEARTBEAT.md 逻辑
- SA 尚未返回结果

### 沈老板 @OPS 其他消息（PM NO_REPLY）
- 17:05 占位符复制功能没生效
- 17:09 管理员后台分组汇总用户数对不上，请求数据为空

### 教训
- QA subagent 在沙箱中无法访问外网 IP，必须用 127.0.0.1/localhost
- QA subagent 会误提交 .venv 等依赖目录，需在 .gitignore 中预防

## 17:00~17:53 截图修复 + 验收标准补充

### Playwright 截图第 3~4 轮修复
- **根因确认**：QA Playwright 脚本通过前端 UI 登录，前端默认走 LDAP（不可用），所有需认证页面被路由守卫拦截回登录页
- **正确方案**：API 获取 JWT token → `context.add_init_script()` 注入 localStorage（token/role/username）→ 再导航截图
- QA subagent 第 3 轮脚本仍失败（test-login 404 + 不存在的路由如 /metrics/*）
- **PM 直接重写 v2 脚本**（`qa/screenshots_playwright_v2.py`）并执行成功
- **19 张截图全部生成**，14 种不同文件大小，关键页面验证通过：
  - `03-dashboard.png` ✅ 个人看板（指标卡片、趋势、模型分布）
  - `09-admin-users.png` ✅ 管理员用户管理（225 人列表）
- **小瑕疵**：16/17 截到邮件预览弹窗（弹窗遮挡 tab）；dashboard 数据为 0（admin 无 ClickHouse 数据）

### 前端路由确认
- 只有 4 个路由：`/`(redirect→dashboard), `/login`, `/dashboard`, `/admin`
- `/admin` 是单页面 + el-tabs（用户管理/分组汇总/配额级别/通知设置/工作时段/用量排行/全局趋势）
- `/dashboard` 是单长页面（指标卡片+趋势+模型分布+明细表）
- **不存在** `/metrics/*`, `/quota` 等子路由

### localStorage 认证 key
- `token`, `role`, `username`（来自 `frontend/src/stores/auth.ts`）
- test-login 端点当前 404（`auth.allow_test_login` 未配置/默认 false）

### 验收标准补充（进行中）
- 沈老板要求：每条测试用例必须有明确的 PASS/FAIL 判定标准
- 已派发 QA：为 40 条用例补充 AC + 写自动化断言脚本 + 执行
- QA subagent: `agent:qa:subagent:03eb70dd-ff0a-4878-8b3a-61f1a082bfc4`
- 交付物：`qa/full-e2e-test-cases-with-ac.md` + `qa/run_e2e_tests.py`

### P0~P3 代码审查修复进度
- OPS 目前只修了 3 项：test-login 配置开关、PG 连接池、剪贴板 HTTP fallback
- P0 CORS `allow_origins=["*"]` 和 Rate Limiting **未修**
- P1 全部 **未修**（admin.py 日志、clickhouse.py 拆分、错误处理、Pydantic）

### OPS 分组汇总 bug 已修
- commit `d945bc8`：改用 ClickHouse 为用户主数据源（225 人 vs PG 27 人）
- 沈老板追加：随机清除 100 用户部门数据测试空部门容错

## 18:32 Pre-compaction flush #3

### QA 字段对齐重跑完成
- QA subagent 修正 AC 字段名（token→token, department-summary→departments 等）
- 重跑结果：**13 PASS / 0 FAIL / 0 BLOCKED / 27 MANUAL**
- 交付物：`qa/run_e2e_tests.py` + `qa/full-e2e-test-cases-with-ac.md`

### OPS session 重启
- OPS 在本群 session 无响应（model timeout），沈老板要求重启
- 删除 sessions.json 中 `agent:ops:feishu:group:oc_8b026` 条目
- 新 session `7ccf2d3a` 自动创建，OPS 恢复响应

### P0~P3 代码修复全部完成 12/12 ✅
- **P0（3/3）**：CORS 配置化(`0cbde53`+`4ded14e`)、test-login 开关(`10d69eb`)、Rate Limiting(`0cbde53`+`1005621`)
- **P1（4/4）**：
  - OPS 用 Claude Code 完成 clickhouse 拆分+admin 日志（commits `6340b5b`, `a1afb34`）
  - PM 派 Dev subagent 也完成了 P1 全部4项（commits `e753e60`, `33d72ed`, `e800875`, `fa7bee9`）
  - Dev subagent 362 tests PASS
- **P2（2/2）**：缓存 TTL 已有、Docker 镜像版本标签(`eebaaf4`)
- **P3（2/2）**：notification v1 废弃声明、TESTING.md(`eebaaf4`)

### QA 全量回归通过
- API：13 PASS / 0 FAIL / 0 BLOCKED
- UI 截图：19/19 全部生成
- 截图脚本顺序修复：排行榜+全局趋势移到邮件预览弹窗前，16/17 不再被遮挡

### 截图验收报告
- 飞书文档：https://www.feishu.cn/docx/NDlYdFQjzojp4mx0hf3clw4CnBe
- 19 张截图全部插入文档，带用例对照标注
- PDF 也生成了：`/data/workspaces/ai-code-usage/qa/screenshots-report.pdf`（838KB）

### 沈老板指示
- P1 用 Ralph（Claude Code）派 Dev 开发，不走 OPS dispatch
- OPS dispatch 路径写错（/data/workspaces/dev/ 不存在），PM 接手用 subagent
- QA 测试由 PM 统一管理，@ OPS 不要重复派 QA
- 沈老板 @OPS 的消息 PM 继续 NO_REPLY

### 注意：OPS 和 PM 的 P1 commits 可能冲突
- OPS Claude Code session 产出：`6340b5b`(拆分), `a1afb34`(日志), `97d3f01`/`d252b86`(错误处理), `a4e58c1`(Pydantic)
- PM Dev subagent 产出：`e753e60`(拆分), `33d72ed`(日志), `e800875`(错误处理), `fa7bee9`(Pydantic)
- 两套 commits 都在同一分支，需要确认哪套生效（git log 显示 PM 的在后面，应该是 PM 的覆盖了 OPS 的）

### 沈老板提议：自动化测试集成到 Ralph
- 问题：能否把测试流程加到 CLAUDE.md，让 Ralph 每次改代码后自动跑
- PM 分析：成本低（exec 调用，每次 5-10K tokens，执行不到 1 分钟），收益高（防回归）
- 现有测试资产：单元 362 + API 13自动/27手动 + UI截图 19页面
- 方案：CLAUDE.md 加 pre-commit hook 指令
- 沈老板确认：两个都加
- Dev subagent 完成：commit `fd89242`，push 到 ralph 分支

### CI/CD + 分支管理
- CLAUDE.md 创建完成（1.1KB）
- GitHub Actions test.yml 创建完成（903B），push/PR 时跑 pytest
- OPS 直接推 main（未走 PR），沈老板要求后续规范
- OPS 正在配置 main 分支保护（必须 PR + 1 reviewer + CI pass + 禁 force push）
- 现有分支：main, fix/p0-p3-security-quality, ralph/ai-code-usage-dashboard, feature/model-pricing 等
- OPS 的 P1 commits 和 PM Dev subagent 的 P1 commits 可能重复（两套都在 main 上）

### PM 漏回复问题
- 沈老板 @PM 问分支情况（18:57），PM 查了但回复被后续消息冲掉
- 19:00 沈老板追问"PM为啥没回复"，PM 补上了分支分析
- 教训：查到结果必须立即回复，不能等

## Sprint 7 收官推进（20:46 更新）

### QA 重测最终通过
- TASK-128 RETEST2 done（10:10 UTC）：AC-16 template outdated 端到端验证通过
- 之前经历 57 轮 cascade-retry 自动重试（R5~R57），根因是 demo-template 无 PUBLISHED 版本
- Dev seed hotfix（commit 19a9ca7）解决后 QA 通过

### 门禁卡点
- TASK-128 QA 通过但门禁 3 项未过：Steps 0/3、Artifacts 无、Schema 格式错误
- 已派 Dev 补全 TASK-128.json 门禁材料（TASK128-GATE-FIX dispatch）
- 通过后：TASK-128→DONE → auto-dispatch 派 TASK-129 PO 验收 → Sprint 7 收官

### 自动化闭环部署完成（OPS）
- 方案 A：auto-dispatch.js 独立 cron（每 10 分钟）✅
- 方案 B：cascade-retry.js 双向闭环（QA→Dev→QA）✅ Dev 补全了 Phase 2
- 方案 C：dispatch-tracker.json 脚本化更新 ✅
- crontab 三条 cron 全部在跑
- 发现问题：cascade-retry 无 max retry 上限，导致 57 轮死循环。待加上限逻辑

### SA 分析：PM 自动派发失效根因
- PM heartbeat 跳过 Step 2（auto-dispatch.js）— LLM 跳步问题
- 与 Dev confirm-only-loop 同类：LLM 跑完巡检报告后提前 HEARTBEAT_OK
- 解法：把确定性操作从 LLM 路径移到 cron（已执行）

### Dev 并行上限调整
- 2→4（沈老板决策 04-17 23:44）
- Hotfix 拆成 4 个独立 dispatch 并行跑：SSRF ✅、Templates 500 ✅、CLI 命令 ✅、template.lock ✅
- CLI login 非交互修复（--username/--password，164/164 tests pass）
- 模板发布链路补全（发布 API + Minio 地址 + Webhook API）✅

### 关键决策
- 沈老板选方案 A：继续让 Dev 补全功能（不降低验收标准）
- 沈老板选方案 B：PM 不亲自下场，让 Dev 补全 cascade-retry 自动化
- cascade-retry 需加 max retry 上限（建议 5 次）— 待派发

## 21:45 每日复盘

### 当日总结
极高产一天。配额邮件 v2.0 从需求→开发→部署→QA 全链路完成。P0~P3 代码审查修复 12/12 全部完成。QA E2E 全量测试（40 用例 + 19 截图）通过。Sprint 7 收官推进中（TASK-128 QA 通过，门禁补全中）。CI/CD + CLAUDE.md 自动化测试集成完成。

### 做对了什么
1. PM 重写 Playwright v2 脚本直接解决 QA 截图问题（4 轮失败后果断接手）
2. 沈老板 @OPS 消息全程 NO_REPLY，记录变更到 memory 不插嘴
3. 用 Dev subagent 并行完成 P1 修复（OPS 也在做，但 PM 方案更快生效）
4. QA dispatch 拆分详细（6 步 + 18 测试用例 + 验收标准）

### 做错/可改进
1. PM 查到分支情况后没立即回复，被沈老板追问（→ LRN-20260421-002）
2. cascade-retry 没设 max retry 上限导致 57 轮死循环（→ LRN-20260421-001）
3. OPS 和 PM Dev subagent 同时做 P1 修复，产生重复 commits（协调不足）

### 新增 .learnings
- LRN-20260421-001: cascade retry 必须设上限（high）
- LRN-20260421-002: 查到结果立即回复（medium）

### 未完成项
- [ ] cascade-retry max retry 上限逻辑（待派发）
- [ ] TASK-128 门禁补全 → TASK-129 PO 验收 → Sprint 7 收官
- [ ] 配额级别人数前后端修复（OPS 进行中）
- [ ] 管理后台刷新报错修复（OPS 进行中）

## 每日反思 (22:00 cron)
- 今日任务数: 15+（配额邮件v2全链路、P0~P3修复12/12、QA E2E 40用例、Sprint 7推进、CI/CD集成）
- 关键学习: cascade-retry必须设上限；查到结果必须立即回复
- 新增 learnings: 2 条（qa.cascade_retry_no_limit, pm.reply_immediately）
- 新增 errors: 0 条
- 备注: 21:45 已有完整复盘，本次 22:00 cron 为补充确认，无新增内容
