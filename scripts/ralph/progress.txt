# Ralph Progress Log
Started: Fri Apr 17 23:55:45 CST 2026

## Codebase Patterns
- Use `get_config()` from app.config for all config.yaml access (hot-reloadable)
- PostgreSQL users table: user_id PK, quota_level defaults to 'L1'
- `upsert_user()` in app/services/database.py for creating/updating users
- JWT tokens created via `create_token()` in app/services/auth.py (8h TTL, cfg fingerprint for admin invalidation)
- Auth dependencies: `get_current_user` / `require_admin` from app/deps.py
- ClickHouse field constants in app/data_schema.py (snake_case + camelCase map)
- All new Python files need SPDX header: `# SPDX-License-Identifier: Apache-2.0`
- Backend quality checks: `mypy app/` (strict) + `ruff check app/` (line-length 100)
- Frontend quality checks: `npx vue-tsc --build`
- Frontend API calls: use `api` from `@/api` (axios instance with token interceptor + 401 auto-logout)
- Auth store persists both token and role to localStorage
- Route guard in router/index.ts: `meta.public` skips auth, `meta.requiresAdmin` requires admin role
- ClickHouse queries via `app/services/clickhouse.py` with `cachetools.TTLCache` (5min, 1024 entries)
- PostgreSQL quota_levels table: level (PK), monthly_token, daily_requests — seeded with L1/L2/L3 defaults
- `get_quota_limits(level)` in database.py returns limits dict; no caching (per spec)
- User's quota level from `users.quota_level`, limits from `quota_levels` table
- Vue template `v-else-if` doesn't narrow nullable refs — use `!` in callback props like `:format`
- `formatWan(n)`: display numbers ≥10000 as "X.X万" — reuse pattern for token display
- Admin router at `/api/admin` prefix; all admin endpoints use `Depends(require_admin)` from deps.py
- Inline table editing pattern: `editingLevel` ref to track which row is being edited

---

## 2026-04-18 00:01 - US-001
- 项目脚手架完成：FastAPI backend + Vue3 frontend + Docker Compose
- Files: backend/app/{main,config,data_schema,routers/health}.py, frontend/src/{App,router,stores,views}*, docker-compose.yml
- **Learnings:** ClickHouse 字段名使用 camelCase（见 data_schema.py），data_schema.py 同时定义 snake_case 和 camelCase 常量
- Passes: true
---

## 2026-04-18 - US-002
- 实现 POST /api/auth/login：bcrypt 验证 config.yaml admins[].password_hash，返回 JWT（8h，含 role=admin）
- JWT payload 嵌入 cfg fingerprint（sha256(password_hash)[:16]），password_hash 变更后旧 token 自动失效
- 新增 backend/app/deps.py：get_current_user / require_admin FastAPI 依赖，验证时对比当前 config fingerprint
- Files: backend/app/main.py, backend/app/deps.py, backend/app/routers/auth.py, backend/app/services/auth.py
- **Learnings for future iterations:**
  - JWT fingerprint 机制：token 里的 cfg 字段 = sha256(当前 password_hash)[:16]，每次验证时重新计算对比
  - 所有需要认证的路由用 Depends(get_current_user)，需要管理员权限用 Depends(require_admin)
  - config 热加载（get_config()）保证每次请求都读最新 password_hash
---

## 2026-04-18 - US-003
- Implemented AD user login via LDAP bind in POST /api/auth/login (unified endpoint: admin first, then LDAP fallback)
- Created app/services/ldap_service.py: LDAP authentication with sAMAccountName search + bind
- Created app/services/database.py: PostgreSQL users table (auto-init on startup), upsert_user with L1 default
- Updated auth router: non-admin usernames trigger LDAP flow, auto-create user in PG, return JWT with userId+role=user
- LDAP unavailable returns 503 with friendly Chinese message
- Files: backend/app/routers/auth.py, backend/app/services/ldap_service.py, backend/app/services/database.py, backend/app/main.py
- **Learnings for future iterations:**
  - Users table schema: user_id (PK), username, nickname, enterprise, mail, quota_level (default L1), created_at
  - upsert_user uses ON CONFLICT DO UPDATE to refresh AD attributes on each login
  - init_db() called on FastAPI startup event to ensure users table exists
  - LDAP search uses ldap.filter.escape_filter_chars to prevent injection
  - deps.py (from US-002) provides get_current_user/require_admin for route auth
---

## 2026-04-18 - US-004
- Implemented login page with Element Plus form (username/password + validation + error display)
- Created `src/api.ts` axios instance with Bearer token interceptor and 401 auto-logout redirect
- Added Vue Router `beforeEach` guard: unauthenticated users redirected to /login, non-admin blocked from /admin
- Auth store now persists role to localStorage alongside token
- Files: frontend/src/api.ts (new), frontend/src/stores/auth.ts, frontend/src/router/index.ts, frontend/src/views/LoginView.vue
- **Learnings for future iterations:**
  - Use `api` from `@/api` for all backend calls — handles auth header and 401 redirect automatically
  - Route meta: `public: true` to skip auth guard, `requiresAdmin: true` to restrict to admin role
  - Element Plus form validation: use `formRef.value?.validate().catch(() => false)` pattern
  - 401 interceptor uses `window.location.href` (not router.push) to ensure full state reset
---

## 2026-04-18 - US-005
- Implemented GET /api/quota/usage returning monthly token and daily request quota bars
- Created app/services/clickhouse.py: ClickHouse client + TTLCache (5min) for monthly tokens & daily requests
- Extended database.py: added quota_levels table (L1/L2/L3 with defaults), get_quota_limits()
- Created app/routers/quota.py: QuotaUsageResponse with used/limit/percent/color/message per bar
- Registered quota router in main.py
- Files: backend/app/services/clickhouse.py (new), backend/app/routers/quota.py (new), backend/app/services/database.py, backend/app/main.py
- **Learnings for future iterations:**
  - ClickHouse table is `events` in `otel` database; field names are snake_case (see data_schema.py)
  - TTLCache key pattern: `"prefix:{user_id}:{date}"` for cache isolation
  - Quota color thresholds: monthly 50/80/100%, daily 80/100%
  - JWT payload has `userId` for regular users, `sub` for admins — use `user.get("userId") or user.get("sub")`
---

## 2026-04-18 - US-006
- Implemented GET /api/metrics/summary?scope=month|today
- Month scope: total_token, request_count, active_days, daily_avg_token
- Today scope: total_token, request_count (active_days/daily_avg_token omitted as null)
- Added ClickHouse helpers: get_monthly_request_count, get_monthly_active_days, get_today_token_usage
- Created backend/app/routers/metrics.py, updated clickhouse.py and main.py
- **Learnings for future iterations:**
  - Metrics router at /api/metrics prefix — future metric endpoints (trend, detail, etc.) go here
  - Optional[int] = None in Pydantic model to omit fields from response when scope doesn't apply
  - Scope enum for query parameter validation (month|today)
---

## 2026-04-18 - US-007
- Implemented quota progress bars and metric cards frontend components
- QuotaProgressBar: calls GET /api/quota/usage, shows monthly token + daily request bars with color coding
- MetricCards: calls GET /api/metrics/summary with scope toggle (month/today), shows 4 cards (month) or 2 cards (today)
- DashboardView updated to compose both components
- Files: frontend/src/components/QuotaProgressBar.vue (new), frontend/src/components/MetricCards.vue (new), frontend/src/views/DashboardView.vue
- **Learnings for future iterations:**
  - Vue template `v-else-if` doesn't narrow nullable refs for TS — use `!` assertion in `:format` callbacks
  - `formatWan()` helper: display numbers ≥10000 as X.X万 — reuse in future components
  - Element Plus `el-progress` with `text-inside` + custom `:format` for showing "used/limit" text
  - Components fetch data on mount via `api.get()` from `@/api` — consistent pattern for dashboard widgets
---

## 2026-04-18 - US-008
- Implemented GET /api/metrics/trend?days=7|30&start=&end= returning daily input/output/total token breakdown
- Added get_daily_trend() in clickhouse.py with TTLCache
- Created TrendChart.vue: ECharts bar/line chart with stacked input/output tokens
- Time filter: 7-day / 30-day presets + custom date range picker
- Chart type toggle: bar (default) / line
- Added TrendChart to DashboardView
- Files: backend/app/services/clickhouse.py, backend/app/routers/metrics.py, frontend/src/components/TrendChart.vue, frontend/src/views/DashboardView.vue
- **Learnings for future iterations:**
  - ECharts init needs `nextTick()` after loading=false to ensure DOM element exists
  - Use `echarts.init(ref)` + `dispose()` in `onBeforeUnmount` to prevent memory leaks
  - ClickHouse `event_date` may return date object — use `hasattr(row, "isoformat")` for safe serialization
  - Trend API reuses same /api/metrics prefix router — add new endpoints to existing metrics.py
---

## 2026-04-18 - US-009
- Implemented GET /api/metrics/detail with time range, model, IDE type filters; grouped by date+model
- Implemented GET /api/metrics/export.csv with 3-month limit validation (returns 400 if exceeded)
- Added get_detail_records() in clickhouse.py with TTLCache and parameterized WHERE clauses
- Created DetailTable.vue: el-table with sortable columns, filter inputs, date range picker, CSV export button
- CSV export uses fetch() with auth header to download blob
- Files: backend/app/services/clickhouse.py, backend/app/routers/metrics.py, frontend/src/components/DetailTable.vue, frontend/src/views/DashboardView.vue
- **Learnings for future iterations:**
  - Detail endpoint reuses metrics router at /api/metrics prefix
  - Server-side sorting via sort_by/sort_order query params with whitelist validation
  - CSV export uses StreamingResponse with io.StringIO buffer
  - Export range validation: (end - start).days > 92 → 400
  - Frontend CSV download: fetch with Bearer token → blob → object URL → click
---

## 2026-04-18 - US-010
- Implemented GET /api/metrics/model-distribution returning per-model token totals with percent
- Added get_model_distribution() in clickhouse.py with TTLCache, grouped by request_model_name
- Created ModelDistribution.vue: ECharts donut (pie with inner radius) chart with time range filter
- Added ModelDistribution component to DashboardView between TrendChart and DetailTable
- Files: backend/app/services/clickhouse.py, backend/app/routers/metrics.py, frontend/src/components/ModelDistribution.vue (new), frontend/src/views/DashboardView.vue
- **Learnings for future iterations:**
  - Donut chart: `radius: ['40%', '70%']` for ring shape, `center: ['60%', '50%']` to leave room for legend
  - Percent calculation done server-side in the router for simplicity
  - Reuses _resolve_date_range() helper from US-009 for consistent date param handling
---

## 2026-04-18 - US-011
- Implemented GET /api/admin/quota-levels returning L1/L2/L3 with monthly_token, daily_requests, user_count
- Implemented PUT /api/admin/quota-levels/{level} for editing limits (rejects non-L1/L2/L3)
- Created backend/app/routers/admin.py with require_admin dependency
- Added get_all_quota_levels() and update_quota_level() to database.py
- Created QuotaLevelManager.vue: el-table with inline editing (edit/save/cancel per row)
- Updated AdminView.vue to include QuotaLevelManager component
- Registered admin router in main.py
- Files: backend/app/routers/admin.py (new), backend/app/services/database.py, backend/app/main.py, frontend/src/components/QuotaLevelManager.vue (new), frontend/src/views/AdminView.vue
- **Learnings for future iterations:**
  - Admin router at /api/admin prefix — future admin endpoints (users, trend, departments, leaderboard) go here
  - require_admin dependency from deps.py handles 403 for non-admin users
  - Inline editing pattern: editingLevel ref + editForm ref, startEdit copies row data, saveLevel PUTs then refetches
  - quota_levels table has fixed L1/L2/L3 rows — no add/delete supported
---
